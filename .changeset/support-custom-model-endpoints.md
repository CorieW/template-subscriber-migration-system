---
"template-subscriber-migration-system": patch
---

Allow direct model calls to use an optional custom HTTP endpoint with a dedicated endpoint API key, while keeping OpenAI Responses API as the default.
