---
"template-subscriber-migration-system": patch
---

Accept approve/revise guidance on the same line as the template sync command and require `OPENAI_API_KEY` before generation, even when using mocked generation output.
